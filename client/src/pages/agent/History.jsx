import { Check, Download, ShieldCheck } from 'lucide-react';
import StatusBadge from '../../components/common/StatusBadge';
import { history } from '../../services/mockData';
export default function AgentHistory() { return <div className="standard-page"><div className="page-intro"><div><span className="section-kicker">Operations / Completed</span><h2>Delivery <em>history.</em></h2><p>A verified record of every household reached.</p></div><button className="button button-ghost button-small"><Download size={15} /> Export log</button></div><div className="history-table-wrap"><table className="history-table"><thead><tr><th>Booking ID</th><th>Customer</th><th>Date</th><th>Status</th><th>Verification</th></tr></thead><tbody>{history.map((item, index) => <tr key={item.id}><td><strong>{item.id}</strong></td><td>{['Ananya Rao','Meera Shah','Vikram Singh','Sana Iqbal'][index]}</td><td>{item.date}</td><td><StatusBadge tone="success"><Check size={12} /> {item.status}</StatusBadge></td><td><span className="verified-label"><ShieldCheck size={15} /> {item.verification}</span></td></tr>)}</tbody></table></div></div>; }
