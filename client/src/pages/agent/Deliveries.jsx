import { ArrowRight, Filter, Map, Search, SlidersHorizontal } from 'lucide-react';
import { Link } from 'wouter';
import { demoDeliveries } from '../../services/mockData';
import StatusBadge from '../../components/common/StatusBadge';
export default function Deliveries() { return <div className="standard-page"><div className="page-intro"><div><span className="section-kicker">Operations / Today</span><h2>Today’s <em>deliveries.</em></h2><p>Four active stops in your demo route. Keep each handoff visible.</p></div><span className="demo-pill"><span className="live-dot" /> Shared demo state</span></div><div className="list-toolbar"><div className="search-field"><Search size={16} /><input placeholder="Search booking or customer" /></div><button className="button button-ghost button-small"><SlidersHorizontal size={15} /> Filters</button><span className="filter-meta"><Filter size={15} /> 4 deliveries</span></div><div className="full-delivery-list">{demoDeliveries.map((item, index) => <article className={'full-delivery-card ' + (index === 0 ? 'is-primary' : '')} key={item.id}><div className="full-delivery-marker">0{index + 1}</div><div className="full-delivery-body"><div className="full-delivery-heading"><div><span className="eyebrow">{item.id}</span><h3>{item.customer}</h3></div><StatusBadge tone={index === 0 ? 'orange' : index === 1 ? 'info' : 'neutral'}>{item.status}</StatusBadge></div><p>{item.address}</p><div className="full-delivery-meta"><span><Map size={14} /> {item.distance}</span><span>ETA <strong>{item.eta}</strong></span></div></div><Link href={index === 0 ? '/agent/active' : '/agent/deliveries'} className="button button-ghost button-small">Open stop <ArrowRight size={14} /></Link></article>)}</div></div>; }
